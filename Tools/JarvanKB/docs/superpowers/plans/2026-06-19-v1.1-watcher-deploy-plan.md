---
task: v1.1-watcher-deploy
lane: fast            # ops/deploy, no watcher-code change; specs/ omitted (deploy is driven by handoff + from-orchestrator-v1.1 letter)
owner: WatcherDeployImpler
parent: RootOrchestrator g5 (converge to docs/sendbox/toOrchestrator/)
created: 2026-06-19
status: in-progress
---

# v1.1 Watcher Deploy — bring up SP-5a (zhihu) + SP-5b (bilibili) as daemons

Source of truth: `docs/sendbox/toWatcherDeployImpler/{handoff.md, from-orchestrator-v1.1-config-and-requirements.md}`.
This is **ops** (config + compose + verify), not a watcher-code change. The watcher source is frozen.

## Stage-0 resolved (live-probed 2026-06-19)
- **Cookie box** = `jasonjarvan` (only account in gitignored `cookie-manager.yaml`; cookies refreshed today; `/get/:uuid`
  returns 200 **without** the auth_token, so zhihu's tokenless provider works). Reuse its creds — not re-asked.
- **Zhihu**: `type: user me` → `/api/v4/me` → real token `zhao-cheng-57-99-79` → **35 collections**;
  default-collection **我的收藏 = `721323262`** (`is_default=True`, 114 items). (`people/self/...` is unreliable; the
  watcher uses the `me`→/api/v4/me path which is correct.)
- **Bilibili**: 22 fav folders (~231 videos total). MiMo provider in BN = `cf11f2fc-42ac-4d44-871d-fbb282a2fe0b`
  (`xiaomitokenplan`, base `token-plan-cn.xiaomimimo.com/v1`). `model_name = mimo-v2.5` (verified resolves: HTTP 200).
- **Backfill policy** = **from-now** (user-decided 2026-06-19): only NEW favorites land going forward; history untouched.

## Deploy-artifact gaps to fix (ops; NOT watcher-code contract)
1. **zhihu Dockerfile**: also `COPY Engine/common` + `pip install` it — `watcher.py` imports `jarvankb_common` at
   module top-level (and `LLMClient` for classify); the image currently installs only `Engine/zhihu` + the watcher,
   so the container would crash on import. (bili needs nothing extra — engine `bilibili`, no `jarvankb_common`.)
2. **zhihu compose**: bind-mount the host vault dir → `/data/output`; mount repo `config/llm.yaml` → `/llm.yaml`
   + `JARVANKB_LLM_CONFIG=/llm.yaml`; pass `MIMO_API_KEY` (env_file repo `.env`). Needed for the classify path.
3. **bili compose**: bind-mount the host vault dir → `/data/output`. (state already persisted; `network_mode: host`.)
4. **`config/llm.yaml`** (gitignored): `mimo` profile model `openai/mimo-v2.5-pro` → `openai/mimo-v2.5` (v1.1 flat model).

## Steps
1. `config/llm.yaml`: mimo → `openai/mimo-v2.5`.
2. zhihu: Dockerfile (+Engine/common), compose (vault mount + llm.yaml + MIMO_API_KEY), write gitignored `config/zhihu-watcher.yaml`.
3. bili: compose (vault mount), write gitignored `config/bilibili-watcher.yaml` (22 folders, engine, cookie); **pre-seed**
   each folder's `state-<id>.json` watermark = now (from-now) so the first production cycle does NOT backfill 231 videos.
4. `docker compose build && up -d` both.
5. **Verify** (`superpowers:verify` discipline, evidence before claims):
   - scoped smoke (temp state+output, `--once` ×2): tiny zhihu collection + a 1-video bili folder → prove land then `0 new` dedup.
   - production daemon: first cycle connects (cookie pull, 35 collections / 22 folders listed, 0 new under from-now). Logs as evidence.
   - confirm `mimo-v2.5` end-to-end on the zhihu classify path.
6. ops-doc-maintainer: record both running services (no listen ports, vault output dirs, deps :48088/:3015).
7. **toUser ops manual** (中文) → `docs/sendbox/toUser/` (start/stop/status/logs/deps/add-folder/failure-behavior/cookie gotchas).
8. Convergence letter → `docs/sendbox/toOrchestrator/from-watcherdeployimpler-done.md` + Dashboard rows.
9. Step-8 RepoMem.merge closure (promote deploy-ops gotchas; commit tracked compose/Dockerfile with explicit pathspec — shared index).

## Hard constraints
- NO creds in any tracked file (configs + llm.yaml are gitignored; assert `git check-ignore` + grep before any commit).
- Shared git index: commit tracked changes with **explicit pathspec** + verify `git diff --cached`.
- If a real watcher-CODE bug surfaces (not a deploy-artifact gap), STOP + escalate, don't fix silently.
