# User Now — Pending Actions

> Single source of truth for "what the user needs to do next" in JarvanKB.
> Authors: any agent session may append. Reader: user.
> Protocol contract: `~/.claude/skills/cc-dashboard/SKILL.md`
> Repo-local hook (language policy, mark-done owner, triggers, **H2A coupling**): `docs/HarnessStack/hooks/cc-dashboard.md`

## Where else to look

> All Human-to-Agent (H2A) interactions surface through `sendbox/` + this Dashboard. RepoMem / superpowers specs / hooks are agent-internal; pointers below let user find them without memorizing paths.

| 想看 | 去哪 |
|---|---|
| SP 状态板（kanban-like） | 本文件 §SP Status Board ↓ |
| Recipe 历史 / 改名记录 / OSS 切分计划 | `docs/RepoMem/persist/version-plan.md`（A2A 英文参考） |
| 当前 active session 的交接信 | `docs/sendbox/to{Prefix}{Role}/handoff.md`（例：`toSP0Impler/`、`toZhihuCrawlOrche/`） |
| 收到的 milestone-done / blocker 信件 | `docs/sendbox/toOrchestrator/from-*.md` |
| 用户单向信（agent → user） | `docs/sendbox/toUser/*.md` |
| 历史决策 / 调研结论 / 架构 | `docs/RepoMem/persist/memory/` 与 `architecture/` |
| 跨模块 design / plan | `docs/superpowers/{specs,plans}/` |
| 调用 JarvanKB 工具的契约（caller agent） | `docs/sendbox/toAgent/handoff.md` |
| HarnessStack v2 治理 | `docs/HarnessStack/longterm.md` |
| 项目总览（人类可读） | `README.md` |

## SP Status Board

依赖图 → 现在能并行什么

SP-0 ✓ ──┬─ SP-1 ✓ ──┬─ SP-3(知乎Skill)   ⚫ done ← SP-2 ✓
         │            ├─ SP-4b(B站Skill)  ← 等 SP-4a
         │            ├─ SP-5a(知乎Watcher)⚫ done(v1/v1.1✓;v1.2→MainOrche;SubOrche收敛)
         │            └─ SP-5b(B站Watcher) ← 等 SP-4a
         ├─ SP-2(知乎引擎) ⚫ done
         └─ SP-4a(B站引擎) 进入条件:SP-0 ✓ + BN docker 可达
         
> JarvanKB 子项目状态板（kanban-like）。一屏看全部 SP 状态 + 责任 agent + 进入条件。
> 状态 emoji：🟡 wip / 🔴 blocked / ⚫ done / ⚪ queued / 🟢 ready
>
> 升级触发（任一满足则迁移到 `antopolskiy/kanban-md`）：≥2 个 impler 并发 / 单 SP 内 task ≥10 / 需要 per-task 评论历史。详见 R9 调研结论。

| ID | 名称 | 状态 | Owner Agent | 进入条件 |
|---|---|---|---|---|
| SP-0 | 骨架 + recipe v2 迁移 | ⚫ done | sp0impler | （无）|
| SP-1 | CookieManager（自写 Express 复刻 CookieCloud 协议 + hook） | ⚫ done | sp1impler | 完成 2026-05-31（merge `b84ee0f`，40 tests，协议契约 `Service/crawl/cookie-manager/docs/interface.md`）；Step 8 RepoMem.merge 已完成（impler-driven HITL，激活 credentials 域） |
| SP-2 | 知乎引擎 | ⚫ done | sp2impler | 完成 2026-06-02（merge `f8c14cb`，51 tests + 真站 smoke 全过；纯 cookie+HTTP 无签名/无浏览器）；Step 8 RepoMem.merge 已完成（impler-driven HITL，提升知乎链路根因/坑到 `crawl-pipeline.md`）；契约 `Engine/zhihu/docs/interface.md`；**v1.1 评论完整树 ⚫ done**（merge `9081cbc`，58 tests + live smoke；Step 8 闭环：decisions.md D7 + 提升 child_comment offset 坑到 `crawl-pipeline.md` §知乎链路，by zhihucommentimpler）；**v1.2 限流加固（原 ARTICLE api-fallback 重定范围）⚫ done**（2026-06-10，merge `f603157` 本地，**67 tests + 真站 smoke**：12/12 专栏 200、限流可观测生效、单 URL 无延迟；non-breaking `fetch()`/`FetchResult` 不变，新增 `zhihu.configure()`。**重定范围**：`/api/v4/articles/{id}` 需 `x-zse-96`（403 code 10003，≠无签名 answers）→ 镜像 ANSWER 兜底不可行、未引签名器（守 D1）；user 决策 A=引擎统一 `_request` 加**主动限流（最小间隔+抖动，进程共享）+ 403/429 退避（遵从 Retry-After）**（风控突发敏感非累计）。Step 8 闭环（impler-driven HITL）：decisions D8（机制留代码）/D9 + 提升 4 条到 `crawl-pipeline.md §知乎链路`（含 **`__zse_ck` 导航页时效门**=专栏抓取真障碍、cookie 点 key `.zhihu.com`、articles 签名门），temp pruned。**残留**：`__zse_ck` 周期过期 → 长期需定期同步 cookie 或引求解器（破 D1，root 决策，deferred → UN-032）。plan `…/plans/2026-06-09-zhihu-ratelimit-hardening-plan.md`；done 信 `toOrchestrator/from-zhihuarticleimpler-done.md`） |
| SP-3 | 知乎 Skill | ⚫ done | sp3impler | 完成 2026-06-07(已合并 feat/agentcrawl-bootstrap,功能 tip `c500119`,**40 单测 + 离线 smoke + live vague_path 分类**对真 provider `mimo-v2.5-pro`(OpenAI 协议)通过)。`URL→md` 纯抓取无 LLM;仅 vague 路径调 LLM 分类(推断现有子目录+提议新建;输入=标题+type+清洗后前导 `classify_snippet_chars` 默认 240 省 token);cookie 主动拉取内存解密(绝不落盘明文);**落地 `Engine/common` LLMClient 真身**(`jarvankb_common`,litellm + 自定义 OpenAI 兼容 provider;SP-4b/6/7 复用勿重写);一份 agentskills.io `SKILL.md` 通吃四端。契约 `Skill/crawl/zhihu-crawl/docs/interface.md`。Step 8 RepoMem.merge 已闭环(impler-driven HITL:提升 cookie 解密参考实现 / LLM 共享层 / SKILL.md 打包 / **LLMService v2 路线** 到 persist)。done 信已发 SubOrche。**v2 候选 = LLMService**(D-SP3-4) |
| SP-4a | B 站引擎 | ⚫ done | sp4aimpler | 完成 2026-06-02：**59 单测全过 + 两条路径对真实 BN+真实B站 smoke 通过**（ASR `BV1GJ411x7h7` / 字幕 `BV1BXQABNE4y`→prefetched→mimo-v2.5-pro；prose 可读文本已验证）。subagent-driven 逐任务两阶段 review + 最终整体 review。契约 `Engine/bilibili/docs/interface.md`；部署件 `deploy/bilinote/`。Step 8 RepoMem.merge 已完成（impler-driven HITL，提升 B站 cookie 域=`bilibili.com`无点 + BN+bcut 运维坑到 `credentials.md`/`crawl-pipeline.md`）。done 信已发 orche | |
| SP-4b | B 站 Skill | ⚫ done | sp4bimpler | SubOrche 已 spawn → `toSP4bImpler/handoff.md`(起会话=UN-025)。结构镜像 SP-3(⚫ done):URL→cookie 拉取→`BilibiliCredential`→**冻结 SP-4a 引擎** `engine.transcribe`+`render`→保存 + vague_path 分类。**跨垂直依赖已解除**:SP-3 已 done、真实 LLMClient(`jarvankb-common`)+ provider `mimo-v2.5-pro` 已配 → **直接复用,勿重写,无 LLM-creds gate**(must-read `persist/memory/llm-shared-layer.md`)。cookie=主动 PULL(`domain=bilibili.com` 无点,SESSDATA;engine interface §5 的 `.bilibili.com` 是过时残留)。live smoke 需 BN(已起 `127.0.0.1:3015`)。SP-4a ✓ **【Stage 1-4 ⚫ 2026-06-07】** RepoMem.read 两层 + compressed brainstorm(5 决策 user 拍板:打包镜像 SP-3 / cookie 复用 / vague_path / 分类输入=标题+总结摘要 / 单文件 render 默认 config 可覆盖)+ design(`docs/.../specs/2026-06-07-SP-4b-bilibili-skill-design.md`)+ 10-task TDD plan(`.../plans/2026-06-07-SP-4b-bilibili-skill-plan.md`)已落 commit `b654662`/`2cfea0f`;plan-ready 已发 SubOrche(`from-sp4bimpler-plan-ready.md`),待 greenlight + 执行模式(默认 subagent-driven)。**cookie 失败非致命**(降级 `credential=None`,异于 SP-3 fail-loud)。`config/bilibili-engine.yaml` 缺失 = **live-smoke 时**的 BN/引擎配置 gate(不阻塞单测)。**【⚫ done 2026-06-10】** 10 TDD task subagent-driven 全绿(**29 单测**)+ 最终整体 review(opus)=ready to merge;merge `2334698` → 本地 feat/agentcrawl-bootstrap(worktree 移除/分支删)。**纯消费者**(零 engine/LLMClient 改动)。Step 8 RepoMem.merge 闭环=**零全局提升**(跨 SP 事实均已被 SP-3/SP-4a 提升)+ 模块 decisions 已记 + temp pruned。契约冻结 `Skill/crawl/bilibili-crawl/docs/interface.md`。done 信已发 SubOrche。SP-4b 已注册 SP-6。**LLM live smoke ✓ 通过**(2026-06-11,`c534d7e`:live_smoke 改 LLM-only 镜像 SP-3 + stdlib 自动加载 .env,实跑真 `jarvankb_common.LLMClient`→`mimo-v2.5-pro` 分类样本→机器学习,LIVE SMOKE OK)。**full transcribe+save 真站 ✓ 通过**(2026-06-11):UN-035 BN-412 已由 bn412impler 修复(根因=playurl 拒匿名需 SESSDATA + BN cookie 单例须重启);**我独立复跑** `BV1GJ411x7h7`:显式路径→`/tmp/sp4b-confirm/explicit.md`(4231B,`transcript_source=asr`,frontmatter+BN mimo 总结)、**vague 路径**→mimo 真分类 `music-videos` 落 `~/KB/bilibili/music-videos/…-2.md`(4505B,`was_vague=true`),cookie 优雅降级生效(`credential=None`)。**SP-4b 全链路 live 验证完结**。注:从 `Engine/bilibili/` 跑时需同设 `BILIBILI_CRAWL_CONFIG` + `JARVANKB_LLM_CONFIG`(引擎/LLM 配置均按 CWD 相对解析,usage-doc 缺口) |
| SP-5a | 知乎收藏夹监听服务 | ⚫ done | sp5aimpler | 完成 2026-06-07(merge `7acacb2`,**32 tests + 真站 smoke**:收藏夹端点 200 无 x-zse-96、168 条→140 落盘、二轮去重 0 新增;28 条专栏因 SP-2 api-fallback 仅覆盖 ANSWER 而 403)。纯 cookie+HTTP、seen-id 去重、BlockingScheduler+compose、cookie=SP-1 主动拉取内存解密(绝不落盘明文)。契约 `Service/crawl/zhihu-watcher/docs/interface.md`。Step 8 RepoMem.merge 已闭环(`4a0b45c`,impler-driven HITL:提升收藏夹端点无签名/offset 分页/专栏-403 坑到 `crawl-pipeline.md §知乎链路`)。done 信已发 SubOrche。**v1.1 ⚫ done**(2026-06-10,merge `0489382`,**58 tests + 真站 smoke** 账号 `zhao-cheng-57-99-79`:by-user 35→33、首轮 baseline 0-new、显式 backfill 2 落盘、去重 0):`targets` schema(collection+user 自动发现)、水位纠正(`favorited_at`←**顶层 `created`**、only_after、首轮 baseline、不早停)、FailureStore 403 退避;模块 Step-8 done。**全局 §知乎链路 提升 A/B/C 已批**(A 收藏时间=顶层 created / B 新 `empirical-api-first.md` / C 模块更正)→ impler 落地给 diff→我验→收敛。**「默认收藏夹自动分类」=新 SP,已 greenlit**(扩 watcher opt-in classify、共享分类器从 SP-3 抽到 `jarvankb_common`、仅默认夹 `我的收藏`、复用 ~33 子目录。**已 brainstorm + dispatch = SP-5a v1.2**(`toZhihuClassifyImpler/`,起会话=UN-036):opt-in 默认关、**抓全文存分类子目录**(专栏全文可抓=SP-2 v1.2 已证,纠正"唯一 excerpt 路径"旧说;excerpt 仅作 403 降级)、**tiered token-frugal 分类**(excerpt→vague?→前 N 字,永不喂全文)、**分类 ledger 在 service 层**=权威本地去重(独立于知乎 API)+ v1.3 同步 forward-compat、共享分类器抽 `jarvankb_common`+vague 输出、重开 no-LLM 边界=附加式记录、**Lane:full**。**v1.3=同步知乎收藏夹分类**(知乎写接口疑似签名门=风险,v1.2 smoke 后开干))。**【ZhihuCrawl SubOrche 收敛 2026-06-14】**SP-3 + SP-5a(v1/v1.1)done;**v1.2(UN-036)+ v1.3 移交 MainOrche**;`toAgent/handoff.md` 调用方契约知乎部分已填(Goal-A);done 信 `toOrchestrator/from-zhihucrawlorche-vertical-done.md`。**v1.2 默认夹自动分类 ⚫ done**(2026-06-18,merge `c426131`,127 测试 + live smoke;opt-in 默认关、共享分类器抽 `jarvankb_common`(带 vague+allow_new)、**ledger=classify 去重主干**、熔断阶梯(~7天)+ attention 双层、**no-LLM 边界 add-only 修正**)。**全局提升待 root g5 批**(version-plan v1.3+签名风险 / llm-shared-layer 共享分类器)=`from-zhihuclassifyimpler-done.md`。**last-mile=WatcherDeploy UN-047** |
| SP-5b | B 站收藏夹监听服务 | ⚫ done | sp5bimpler | SubOrche 已 spawn → `toSP5bImpler/handoff.md`(起会话=UN-026)。结构镜像 SP-5a(⚫ done)的 7 组件 daemon(BlockingScheduler+compose+`--once`);调 **冻结 SP-4a 引擎** → BN 转录;默认 15–30min;cookie=主动 PULL(`bilibili.com` 无点);**impler 务必先读 `crawl-pipeline.md` §B站链路**;live smoke 需 BN(已起 `127.0.0.1:3015`)。**水位特例(用户 2026-06-07 指令)**:不许照 SP-5a armchair 读 API——SP-5a 误读 `content.created`(创作时间)漏了顶层 `created`(收藏时间)而错判"无可靠收藏时间"改走 seen-id。SP-5b **Stage 0 先真站爬 favorites API → 落字段语义文档 → 用户 review/改 → 才实现水位**;默认 `fav_time` 高水位(spec + 用户先验),不可靠才回落 seen-id。SP-4a ✓ **【Stage 0 ⚫ 2026-06-10】真站实证(账号真 cookie)证实 `fav_time` 真存在且≠`pubtime`(铁证:fav 04-24 vs pub 04-16)→ 默认 fav_time 水位成立,纠正 SP-5a 臆断;`order=mtime`=fav_time 降序(可早停);坑=`media_count` 含失效条目,停止跟 `has_more` 勿用总数。字段文档待 user review=UN-029,门不阻塞读代码但阻塞 design 定稿** **【UN-029 已清 + design+plan 已批 2026-06-10】**用户裁决:水位=`fav_time`高水位+`bvid` seen-id 兜底、只处理 `type==2`、首发监听 `AI生成`(2216104467)+`编程折腾`(1195057867)、单文件输出。design `6c27afc` + plan(Lane:full,12 TDD task)`0d47338` 已提交批准;plan-ready 已发 SubOrche。**下一步=worktree + subagent-driven TDD 实现**,verification=live `--once` smoke(需 BN 已起 + 新 cookie + 真 config=USER ops) **【实现完成+验证 2026-06-10】**worktree `sp5b-bilibili-watcher`(本地分叉),subagent-driven 12 task 全过、**44 单测+集成**;两阶段 review 逐簇 + 最终 review=READY。**live smoke**:cookie/favorites/`fav_time`早停/引擎调用/**§5 失败降级水位 live 实证**/daemon 不崩 全验;**唯转写成功被 BN yt-dlp `HTTP 412` 风控卡住=UN-035(垂直级 ops 门,非 SP-5b 代码,SP-4b 同担)**。待 user 定 finishing(merge→`feat/agentcrawl-bootstrap` 本地 / 等 BN 修)。Step 8 merge 待 finishing 后(impler 闭环:晋升 fav_time/分页/排序到 §B站链路) **【⚫ DONE 2026-06-11】** merge `51557db`(本地 feat/agentcrawl-bootstrap,worktree 移除/分支删);**Step 8 闭环**=晋升 §B站链路 `5d4a13d`(SubOrche R1-R3:favorites API 新子节 fav_time/order=mtime/has_more 分页 + BN 消费者两坑 ALL_PROXY/412,指向 `empirical-api-first.md`)+ 模块 D1-D8 + temp pruned;契约冻结 `bilibili-watcher/docs/interface.md`;done 信已发 SubOrche。**转写成功路径被 BN-412 卡=UN-035(垂直共担,SP-4b 同,非本 SP 代码)** **【BN-412 已修+SP-5b live 全通 2026-06-11】**BN412Impler 修复(根因=playurl 拒匿名 412;推 cookie+**重启 BN 容器**;我 06-10"推 cookie 不解"是假阴性=没重启)→ UN-035 archived。SP-5b **零代码改动**重跑 `--once` 真实落盘:`编程折腾` 最新 BV1VRfcBrERy 经引擎(字幕路径→mimo 总结)落 14.7KB md、`1 new` 水位进 1772042686;二轮 `--once` early-stop `0 new`(去重不变式 live 确认)。**SP-5b 端到端成功路径已验证** |
| SP-6 | CrawlMdSaver Skill（爬取-笔记整合） | 🟡 dispatched | → SP6Impler | **已解锁**（SP-3 ✓ + SP-4b ✓ 双 done）→ root g4 直属子 SP6Impler 已派（UN-038，**非 SubOrche**，跨垂直注册两个 crawl skill）。范围=注册 SP-3/SP-4b、输入 URL+笔记 / 源文档+笔记、输出合并 md（frontmatter 含笔记元数据）、"整理"步复用 `jarvankb_common` LLMClient。**Lane: full**；纯消费者（勿改两 skill/引擎）。impler 跑全 v2 8 步含自己的 brainstorm（路由/merge-vs-write/frontmatter schema 待与 user 定）。done 后**解锁 SP-7** | `docs/sendbox/toSP6Impler/handoff.md` |
| SP-7 | Thino 块解析整理服务 | ⚪ queued | (无) | SP-6 实现完成 |
| SP-8（v1+） | Web Search Router（透明多-provider router：Exa[Agent-Reach mcporter]+Tavily+知乎垂直+keyless 兜底 SearXNG/DDG；3371★ anysearch=可选黑盒后端非核心） | ⚪ queued | **ReachOrche**（搜索=reach 域，2026-06-18 接管） | v1 全部完成；知乎 API key 已获取。**scope 已细化**：`docs/sendbox/toReachOrche/sp8-search-router-scope.md`（蓝本=多-provider router 范式，设计参考 `dhruv-anand-aintech/anysearch` 2★ 仅借范式不依赖其代码——code-dive user-gated 未决）。**NOT handoff-ready**：①v1 未完（搜索紧迫性绑 v3 deep research 非 v1）②未 brainstorm ③蓝本未核实 |

详细路径见 `docs/superpowers/specs/2026-05-31-SP-0-jarvankb-skeleton-design.md` §7。

## 里程碑（能力轴 v1→v5+）

> 用户视角的能力里程碑（2026-06-18 用户定义），区别于 §SP Status Board 的 SP-N 实现轴。一个里程碑可跨多个 SP；耐久版（A2A）在 `version-plan.md §Capability Milestones`。
> **门规则（orche 常驻）**：每个任务完成都查"是否将解锁某里程碑"；里程碑解锁/临近达成时，orche **handoff 一个 impler 带你走最后一公里**（到能力交付，不止 SP 技术 done）。详 `CLAUDE.md §3` / `longterm.md §Milestone Gating (v2)`。

| 里程碑 | 能力 | 承载 SP/UN | 状态 |
|---|---|---|---|
| **v1** | Hermes 对话：对话里发的知乎/B站内容 + 我的思考 → 总结 → 存 Obsidian | SP-6（在飞）+ 净新"Hermes 对话式 ingest"SP + 可能 MCP 门面（UN-043）；候选 owner=ReachOrche（charter 待定） | 未启动（核心依赖在飞） |
| **v1.1** | 自动监听爬取我的知乎/B站收藏夹 → Obsidian | SP-5a+SP-5b watcher（done）+ SP-5a v1.2 默认夹自动分类（UN-036，2026-06-18 合并）+ WatcherDeploy（UN-047） | 近完成（候你给 vault 根） |
| **v1.2** | 监听处理 Obsidian Thino 笔记里的知乎/B站链接，同 v1 总结保存 | SP-7 ThinoIngester（候 SP-6 落地解锁） | queued |
| **v2** | 监听爬取我关注的人的内容 + 过滤保留有价值信息 → 存 | 净新（扩 watcher 家族 + 过滤层）；**待 brainstorm**：哪些人（如按 B站关注分组）/ 过滤=硬指标+LLM | 未启动 |
| **v3** | deep research 能力 | `Skill/research/MiroResearch` 换 apodex API + 依赖本机 MCP（UN-043）；接 MiroThinker Path A（UN-042） | 未启动（MCP 门面是门） |
| **v4** | 记忆系统 | 净新；接 FutureFeatures（UN-040） | 未启动 |
| **v5+** | research agent 长程任务 | 净新；接 FutureFeatures（UN-040） | 未启动 |

## Active

| ID | Type | Action | Where (detail) | Blocker for | Since | Status |
|---|---|---|---|---|---|---|
| UN-006 | F | 决定 v1.0 GitHub Organization 名（候选：JarvanKB / Jarvan / JarvanWorks）— 此项非阻塞 v1 实现，可推迟到 v1 完成度临近 | `docs/RepoMem/persist/version-plan.md` §v1.0 OSS release plan | v1.0 切分 | 2026-05-31 | open |
| UN-008 | D | Review CodeTeam#1（含 SubOrche 泛化评论）+ CodeTeam#2（HarnessStack v2 consolidated proposal），决定推动上游修复节奏还是先在本仓库本地约定中沉淀 | https://github.com/JasonJarvan/CodeTeam/issues/1 | 后续 sub-project 一致采用 `to{Prefix}{Role}` 命名 | 2026-05-31 | open |
| UN-031 | F | **下次起 JarvanKB 会话时批准 codegraph MCP server**：`.mcp.json` 已加 `codegraph`（stdio，`codegraph serve --mcp`，机器本地不入库）；Claude Code 对项目级新 server 会弹一次 approve——批准后 step-1 符号查询走 MCP（CLI 已可用，不批准也不阻塞）| `.mcp.json` + `docs/HarnessStack/longterm.md` §Harness Enhancement Layer (v2) | 无（增强项） | 2026-06-10 | open |
| UN-034 | B | **起 GrillDocsImpler session**（新会话，cwd=`Tools/JarvanKB/`），第一句：`read docs/sendbox/toGrillDocsImpler/handoff.md and start`。**装社区原版 grill-with-docs 替换当前适配版（设计评审门→CONTEXT.md 生成器）+ 调和被它引用的治理（CLAUDE.md/longterm 的 #4 prop4 设计门，brainstorm 定丢弃 or 换名保留）+ 在项目根跑出 `CONTEXT.md`**。root g4 直属子，完报 `toOrchestrator/`。注：此举**部分推翻上一轮 CodeTeam #4 prop4**（user 已知并裁决）；首个 gate=定位社区原版真源，不确定则问你 | `docs/sendbox/toGrillDocsImpler/handoff.md` | grill-with-docs 语义替换 + 项目 CONTEXT.md | 2026-06-10 | open || UN-026 | B | **起 SP5bImpler session**（新会话，cwd=`Tools/JarvanKB/`），第一句：`read docs/sendbox/toSP5bImpler/handoff.md and start SP-5b`。镜像 SP-5a;**先做 Stage 0 真站 favorites-API 调研 → 字段文档待你 review/改 → 才实现水位**(纠正 SP-5a armchair 读 API);与 SP4bImpler 并行 | `docs/sendbox/toSP5bImpler/handoff.md` | SP-5b 落地 | 2026-06-07 | open |
| UN-029 | D | **Review + 编辑 SP-5b Stage-0 字段文档**(B站收藏夹 API 实证):确认 ① 监听哪些收藏夹(账号下 22 个) ② 水位形态(`fav_time`高水位+seen-id兜底**(推荐)** / 纯 fav_time / 纯 seen-id) ③ 是否只处理 `type==2` 视频。**实证已证 `fav_time` 真存在且≠pubtime(纠正 SP-5a 臆断)**;改完文档或在 chat 拍板即解门,我据此设计水位。门**不阻塞**我读代码,但**阻塞** Step 2 design 定稿 | `Service/crawl/bilibili-watcher/docs/RepoMem/temp/sp5b-bilibili-watcher/favorites-api-fields.md`（信件 `docs/sendbox/toBilibiliCrawlOrche/from-sp5bimpler-blocker-fav-api-review.md`） | SP-5b 水位设计 | 2026-06-10 | **done**(用户裁决:fav_time+seen / type==2 / AI生成+编程折腾 / 单文件;design+plan 已批,进展走 §SP Status Board SP-5b 行) |
| UN-032 | D | **(非阻塞,deferred) 定 `__zse_ck` 导航页反爬的长期处理**：知乎导航页 HTML 需新鲜 `__zse_ck`，过期→403 反爬挑战页（SP-2 v1.2 真站确认），限流/重试治不了。当下靠 cookie-manager 从你浏览器同步新鲜 cookie 即可用（已验证 12/12 专栏 200，且 cookie key 须用带点 `.zhihu.com`）。长期 watcher 选型：(a) 定期同步新鲜 cookie（够用、零新依赖）/ (b) 引 `__zse_ck` 求解器/无头浏览器（破 D1「无浏览器/签名器」，同 zse-96 signer 争议，root 决策）。无紧迫，v1+ | `docs/RepoMem/persist/architecture/crawl-pipeline.md §知乎链路` + `docs/sendbox/toOrchestrator/from-zhihuarticleimpler-done.md §3` | 专栏长期稳定抓取 | 2026-06-10 | open |
| UN-040 | B | **起 FutureFeaturesImpler session**（新会话，cwd=`Tools/JarvanKB/`），第一句：`read docs/sendbox/toFutureFeaturesImpler/handoff.md and start`。**调研+需求澄清两个 v2+ 未来特性**(用 superpowers brainstorm + grill):**A(通用)**=让定时 Agent(Hermes/OpenClaw)成 auto-researcher，接 `/goal`(调研 codex/claude-code 的 goal 实现)自主达成、自动读知识库+用工具(Hermes 工具/skill、本仓库知乎+B站爬取、MiroThinker `MiroResearch` 等);**B(特定)**=给全仓库生成 agent-friendly index(LLM wiki / Mem0 / MemOS…)。**关系**:A 通用、B 特定,A 可从 B 实践中长出(自进化:先调研→总结→完成 B 本身)。**产 draft 写入 `version-plan.md`(标记 draft，将来激活细化),不实现**。注:MiroResearch 未在本地树需先 sync 父仓库;Lane:full。root g4 直属子 | `docs/sendbox/toFutureFeaturesImpler/handoff.md` | 两个 v2+ 特性 draft 入 version-plan | 2026-06-11 | open |
| UN-041 | B | **⛔ DEFERRED 未来任务（勿现在起）：把 JarvanKB 抽成独立仓库**。将 `awesome_agent_tools/Tools/JarvanKB/` 抽出为独立 repo `JarvanKB`，awesome 仅留**链接**（submodule，同现有 gstack/ops-doc-maintainer 等）。已备 deferred handoff + version-plan 记录。**激活条件**：v1 完成（SP-6/7 还没）+ Org 名定（UN-006）+ 你显式发话。关键坑已记：全部历史在 `feat/agentcrawl-bootstrap`（未并 main）→ `filter-repo --subdirectory-filter Tools/JarvanKB/` 须针对该分支、用镜像克隆勿原地改、gstack 不碰。**与 §9 per-SP fractal 的关系待激活时 brainstorm 厘清**（替代 or 先于）| `docs/sendbox/toRepoSplitImpler/handoff.md` + `version-plan.md §v1.0 OSS release plan` | v1.0 仓库独立化 | 2026-06-11 | deferred |
| UN-042 | D | **决定 MiroThinker 接入路径 A vs C**（MiroResearchImpler 实测：hosted mirothinker 是封闭 server-side agent，忽略 caller tools，唯一合规外部工具通道 `mcp_servers` 当前 `403`=beta 未开）。**(A) 订阅路径**=等 MiroMind 开 `mcp_servers` beta→把 anti-crawl MCP 挂到 hosted 模型(用订阅、运维最轻、需 beta+公网可达);**(C) 自托管**=MiroFlow+三方大脑(Claude/GPT/Gemini)+本仓库 anti-crawl MCP(今天可跑、不耗订阅)。item-2 MCP 是各路共需的无悔第一步。**【UPDATE 2026-06-15 — Miro 回复】**(a) 同意**开通 `mcp_servers` beta**(Path A 解锁);(b) **非 `-h` 模型支持 client-side tool call**(尊重你传的 `tools`、发 `tool_calls` 让你客户端执行)→ 新增 **Path A′**(非 `-h` 模型+本地工具,用订阅、免 beta、你掌循环)+ **可能复活 Path B**(自托管 MiroFlow + 非 `-h` API 当大脑 —— 原"B 死刑"只针对 deepresearch 无视工具)。**已向 Miro 发 3 个追问**(API=纯模型 vs 带 harness/哪个是非 `-h` model;自托管 harness 指向 API 时 client-side tools 是否真触发=B 复活?;此用法是否正常计订阅),回复后定 A/A′/B。详见 `version-plan §MiroThinker UPDATE 2026-06-15`。**Item-2 设计据此=同一套 crawl 逻辑既出 MCP server 也出 OpenAI tool-schema**(通吃 A/A′/B) | `version-plan.md §MiroThinker…` + `docs/sendbox/toMiroResearchImpler/miromind-repos-and-anticrawl.zh.md` | MiroThinker 接入定型 | 2026-06-13 | **RESOLVED 主路=Path A**（Miro 2026-06-15 详答:模型 `apodex-1-0-deepresearch{-h,,-mini}`,`-h`=multi-agent 不支持 mcp/client-tc;**平台无裸模 API→A′ 经 API 不存在**;client-tc 仅自部署权重=B′;`mcp_servers`=公网远程 MCP、内测无 URL 白名单。**Path A(托管非 -h workflow + mcp_servers)=唯一用订阅且官方支持**;B′=本地零暴露但 GPU+不吃订阅)。剩待确认 6 项(model id apodex vs mirothinker / 两白名单 / mcp 是否真触发 / schema / Miro 出口 IP / B′ 取舍)。**【2026-06-15 user 选定 Path A】** 非 A 路(B 死/B′ 自起权重/C 三方大脑)已总结归档为 fallback 参考(仅 Path A 出问题时用):`docs/sendbox/archive/mirothinker-fallback-paths.md`(唯一指针)。MCP 鉴权安全设计供 impler 消费:`docs/superpowers/specs/2026-06-15-mcp-facade-auth-security-design.md`。详见 `version-plan §MiroThinker DECISION 2026-06-15` |
| UN-043 | B | **(可立即起，无依赖) 把本仓库 anti-crawl 能力包成 MCP server**：将 zhihu-crawl/bilibili-crawl + 爬取服务包成合规 MCP(结构化输出 content/source_url/fetch_method/auth_context/failure_reason/needs_human_action;合规边界=仅授权 cookie+限流+缓存+溯源、CAPTCHA/墙转人工，**不**绕验证码/不规避访问控制/不轮换身份)。**A、C 两路都需→不依赖 Miro 回复**,无悔先手。说一声我就派 AntiCrawlMcpImpler(root 直属子)。**定型(2026-06-14)**:落点=新 `Service/mcp/` 子类(user 选 option 2);定位=**仓库 MCP 门面 v1**(先 crawl tools,可增量长成全仓库门面,非 god-process);粒度=Engine/Skill→请求响应 tool、watcher→只暴露 control tool;消费经验法则=有状态/共享后端(crawl+anti-crawl)走 MCP、纯指令(research/grill)留 Skill。**🔴 硬约束(2026-06-15,因 Path A 须公网+Miro 无 URL 白名单+用授权 cookie)**:MCP 必须 ① cookie 绝不进出接口面(localhost cookie-manager 内存拉取、用完即弃、绝不回显)② 能力最小化只 URL→markdown(无裸 cookie/任意请求/代理工具)③ 强 per-request 鉴权(bearer/OAuth over HTTPS,可轮换)④ Nginx+TLS 后 + 可选只放行 Miro 出口 IP ⑤ 域名白名单 zhihu/bilibili + 限流 + 审计。(B′ 则工具全本地零暴露,免这套)。**【设计已就绪】** 完整鉴权/安全设计(binding)= `docs/superpowers/specs/2026-06-15-mcp-facade-auth-security-design.md`,impler 起会话即消费;**user 已选 Path A**;尚未派(等 Miro 6 项确认,见 UN-042) | `docs/superpowers/specs/2026-06-15-mcp-facade-auth-security-design.md` + `version-plan.md §MiroThinker` item 2 | 给 MiroThinker/通用 agent 提供 anti-crawl 抓取 | 2026-06-13 | **owner→ReachOrche**（2026-06-18，user 批准的 reach 层边界；ReachOrche 实派前与 root 对齐 MCP 契约；**不再 root 直派**。dispatch gate 仍=6 项 Miro 确认 + user go） |
| UN-038 | B | **起 SP6Impler session**（新会话，cwd=`Tools/JarvanKB/`），第一句：`read docs/sendbox/toSP6Impler/handoff.md and start`。**SP-6 CrawlMdSaver**（`Skill/ingester/crawl-md-saver/`）=注册 SP-3/SP-4b 两个 crawl skill 的整合包装层:输入 URL+笔记 / 源文档+笔记 → 路由抓取 → 合并笔记 → 输出 md（frontmatter 含笔记元数据）。**root g4 直属子**（跨垂直，非 SubOrche）；**Lane: full**；纯消费者勿改两 skill；"整理"步复用 `jarvankb_common` LLMClient。impler 跑全 v2（自带 brainstorm 定路由/merge-vs-write/frontmatter）。**done 后解锁 SP-7** | `docs/sendbox/toSP6Impler/handoff.md` | SP-6 落地 → 解锁 SP-7 | 2026-06-11 | open |
| UN-036 | B | **起 ZhihuClassifyImpler session**（新会话，cwd=`Tools/JarvanKB/`），第一句：`read docs/sendbox/toZhihuClassifyImpler/handoff.md and start SP-5a v1.2`。**SP-5a v1.2 默认夹自动分类**(opt-in 默认关、抓全文存分类子目录、tiered token-frugal 分类、**分类 ledger 在 service 层**=权威去重+v1.3 forward-compat、共享分类器抽 `jarvankb_common`)。**Lane:full**(跨 Engine/common+Service+Skill);impler 跑完整 v2 8 步(含自己 brainstorm/spec/plan + Step-8)。parent=**MainOrche**(ZhihuCrawl SubOrche 已收敛 2026-06-14,v1.2 移交) | `docs/sendbox/toZhihuClassifyImpler/handoff.md` | SP-5a v1.2 落地 | 2026-06-11 | **plan-ready 2026-06-14**(brainstorm+spec+10-task TDD plan 已落+commit;design `b050865`/plan `fc45ab6`/plan-ready 信 `e20155c`;5 项 user-locked 模块内决策见 plan-ready 信)→ **⚫ DONE 2026-06-18**:subagent-driven(11 feature commit + 逐任务两阶段 review + 最终整体 review=READY)、**127 离线测试**(common 22 + SP-3 回归 27 + watcher 78)+ **live smoke**(真`我的收藏`+真 mimo:classify→`赚钱-*`、ledger、二轮 **0 re-hit**、vague→**Tier-2**、坏 key 优雅降级);merged `c426131` 本地 feat/agentcrawl-bootstrap(worktree/分支已清)。Step-8:模块 decisions + `.gitignore` 密钥卫生 + temp pruned;**全局提升(v1.3 路线+`x-zse-96` 写接口签名风险→version-plan;共享分类器→llm-shared-layer)提请 root g5 批**=`toOrchestrator/from-zhihuclassifyimpler-done.md`。**last-mile=部署 UN-047**(WatcherDeploy 接 classify-mode) |
| UN-044 | D | **批准主机服务端口约定**(BilibiliCrawl SubOrche 已发 root 决策信):user 要求所有服务用 45xxx;实测 45xxx 被外来 node(`127.0.0.1:45397`)占 → 按 fallback 取其他 4yxxx。**推荐正式化 48xxx**(48000–48999,1000 口)=现有文档约定(cookie-manager 48088 + frp 公网 48098)、**零迁移**;备选 46xxx(需迁 cookie-manager+frp+扩展地址)。root ratify + 同步 `CLAUDE.md`/`credentials.md`/ops-doc。注:watcher 不监听端口(规则仅约束监听型服务) | `docs/sendbox/toOrchestrator/from-bilibilicrawlorche-port-ratelimit-decisions.md` D1 | 全服务端口规范 | 2026-06-14 | **resolved**(root g4 ratify **48xxx**;已正式化子分配表入 `credentials.md §Host service-port convention`;ops-doc 已有 48xxx 规则,子表由 WatcherDeploy/ops impler 同步) |
| UN-045 | D | **限流/反风控放哪 + 是否现在派**:SubOrche 答=放 **SP-4a Bilibili 引擎**(`_request` 单一收口,镜像 SP-2 v1.2,非 watcher/skill,**预防性**——BN-412 是匿名拒绝+cookie 单例非限流问题)。user 已 greenlight。引擎=root 所属冻结、出我 scope → **待定:root 派 SP-4a v1.x / 或你授权我直接派**。已发 root 决策信 | `docs/sendbox/toOrchestrator/from-bilibilicrawlorche-port-ratelimit-decisions.md` D2 | B站引擎限流加固 | 2026-06-14 | **resolved**(user 选:授权我直接派 → 起会话 UN-046) |
| UN-046 | B | **起 BiliRateLimitImpler session**（新会话，cwd=`Tools/JarvanKB/`），第一句：`read docs/sendbox/toBiliRateLimitImpler/handoff.md and start`。**SP-4a B站引擎限流加固**(镜像 SP-2 v1.2:`metadata/subtitle/bilinote_client` 收口加主动限流+抖动+429 退避遵从 Retry-After;**非破坏**冻结契约;加 `bilibili.configure()` 旋钮)。**预防性**(非修 BN-412)。user 授权派;**parent=MainOrc**(BilibiliCrawl SubOrche 2026-06-14 收敛,re-parent → 报 `toOrchestrator/`);TDD。GO,待 MainOrc 协调到 done | `docs/sendbox/toBiliRateLimitImpler/handoff.md` | B站引擎限流加固 | 2026-06-14 | open |
| UN-047 | B | **起 WatcherDeployImpler session**（新会话，cwd=`Tools/JarvanKB/`），第一句：`read docs/sendbox/toWatcherDeployImpler/handoff.md and start`。**起两个 watcher 常驻服务 + live 验证**(SP-5a 知乎 / SP-5b B站):ops/部署,**Stage 0 先跟你确认 vault 输出根**(默认 `~/KB/{zhihu,bilibili}/`)+ 收藏夹/间隔/SP-1 连接 → 填 config → `compose up -d` → 验首轮落盘+二轮去重。不改代码;**parent=MainOrc**(SubOrche 2026-06-14 收敛,re-parent → 报 `toOrchestrator/`) | `docs/sendbox/toWatcherDeployImpler/handoff.md` | 两 watcher 上线 | 2026-06-14 | **GO — deploy now**(root g4 裁定:watcher 与 MCP **正交**、互不 gate——watcher=自治后台 vault 填充,MCP=按需 agent 抓取,两者并行;详见上轮 chat + SubOrche 分析)。**唯一剩余 gate=你给 vault 输出根**(默认 `~/KB/{zhihu,bilibili}/`)+ 收藏夹/间隔/SP-1 连接。给了即 `compose up -d`)。**【vault 根已给 2026-06-18】** vault root=`/home/shenzhou/Documents/Obsidian/JasonJarvan`;`output_dir`:知乎=`{vault}/ResourceBase资源库/Zhihu知乎`(zhihu example 已是此值)、B站=`{vault}/ResourceBase资源库/BiliB站`(把 bili 占位 `/data/output` 改成它);Docker 里需把这两个宿主目录 bind-mount 进容器。**剩余 Stage-0**=cookie box `uuid`/`password`(48088 SP-1 盒)+ 监听哪些 `targets`/`folders`(知乎默认夹 `我的收藏` 是否 `classify:true`,v1.2 已合)+ 确认 `poll_interval`(知乎 45 / B站 20 min)+ BN `provider_id`/`model_name`(impler 可从 `GET /api/get_all_providers` 自取)。可起 impler 执行 |
## Archive

| ID | Action | Done | By |
|---|---|---|---|
| UN-049 | 让 ReachOrche SubOrche 上岗 + vertical 边界对齐 — **done**：ReachOrche 已 onboard，发 `from-reachorche-ack.md`——scope=**reach/内容供给层**（crawl 引擎/skill/watcher + 未来平台、SP-8 搜索、反爬/认证、UN-043 MCP 门面），**非 v1 Hermes 垂直**（我初始猜测被 user 纠正）。root 已反映：version-plan 加 **ReachOrche g1** 行 + reach 层边界；**UN-043 owner→ReachOrche**；deconfliction 已批（在飞 UN-036/046/047 在 root 收口、domain forward 归 ReachOrche；SP6/GrillDocs/FutureFeatures 留 root），回信 `toReachOrche/from-orchestrator-deconfliction-ratified.md`；两封 reach 边界信已 burn | 2026-06-18 | user + orche g5 + ReachOrche |
| UN-048 | 起 g5 root orche session 继承编排 — **done**：g5（Claude Opus 4.8 1M）已读 `g5-handoff.md`、跑完引导检查（`git log -12` + `toOrchestrator/` 收件箱无新 `from-*` 信 + 四大 must-read：CLAUDE.md / longterm v2 / Dashboard / version-plan），确认 SP-0…SP-5b done、SP-6 在飞、6 在飞 impler + 1 待派（AntiCrawlMcp）状态、MiroThinker 已 **RESOLVED=Path A 主路**（非 §4 letter 的"待 Miro 回复"旧态）。g4 退场，g5 接管治理 + 6 在飞 impler + SP-6→SP-7 排程；`g5-handoff.md` 已 burn，version-plan generations g5 行已标识（2026-06-14→active）。注：分支领先 origin 2 提交（g4 未推）= g5 候 sync | 2026-06-15 | user + orche g5 |
| UN-039 | 起 Codex session 做 impler onboarding — **done**: installed/load-verified `repo-mem`, `sendbox-protocol`, `cc-dashboard`, and project `grill-with-docs` for Codex; read Codex tool map + `CLAUDE.md` + HarnessStack v2; enabled Codex collab; added thin `AGENTS.md`; wrote readiness report `docs/sendbox/toOrchestrator/from-codeximpler-ready.md`. OpenSpec caveat acknowledged: do not use `openspec-*` in JarvanKB v2. | 2026-06-11 | codeximpler |
| UN-035 | 修 BN 下载器 HTTP 412 — **done(根因实证+修复+防复发)**:playurl 对**匿名请求一律 412**(与出口 IP、yt-dlp 版本无关,单变量矩阵证);「推 cookie 不解」=**假阴性**(BN `BilibiliDownloader` 导入时单例只在启动读一次 cookie → 推后须重启容器)。修复=推 cookie+重启;**SP-4b 双路径(subtitle+asr)真实落盘验证**。防复发=宿主 cron `bn-cookie-sync.py`(SESSDATA 变化才推+重启)。提升:runbook §5 SOP + deploy README 坑 + §B站链路 实证根因(HITL 已批,`21c15fa`+`a574ea1`) | 2026-06-11 | bn412impler |
| UN-037 | 起 BN412Impler session — **done**:systematic-debugging 全程(复现→对照矩阵→单变量→修复→E2E 验证);候选 #1 换 IP / #2 升 yt-dlp 均被否证,真因=匿名 playurl 风控+BN cookie 单例。Step-8 闭环(HITL 已批)。done 信 `from-bn412impler-done.md` 已发 SubOrche | 2026-06-11 | bn412impler |
| UN-033 | 跑 SP-4b live 真站 smoke — **done(LLM 部分)**:live_smoke 改 LLM-only(镜像 SP-3)+ stdlib 自动加载 .env;实跑真 `jarvankb_common.LLMClient`→`mimo-v2.5-pro` 分类样本→机器学习(`c534d7e`,LIVE SMOKE OK)。full transcribe+save 真站撞 **BN-412 下载器风控**(B站垂直共担)→ 归 UN-035,非本 Skill 代码 | 2026-06-11 | sp4bimpler |
| UN-025 | 起 SP4bImpler session — **done**:SP-4b 完成 v2 8 步全流程(brainstorm 5 决策→design→10-task TDD plan→subagent-driven 实现 29 测试→最终 review ready→merge `2334698` 本地 feat/agentcrawl-bootstrap)。纯消费 SP-4a 引擎 + jarvankb_common。Step 8 零提升+prune 闭环。live smoke GATED(creds/engine-config)转 UN-033。进展见 §SP Status Board SP-4b 行(⚫ done) | 2026-06-10 | user + sp4bimpler |
| UN-024 | 起 ZhihuWatcherV11Impler session(SP-5a v1.1)— **done**:v1.1 done+merged `0489382`(58 tests + 真站 smoke 账号 `zhao-cheng-57-99-79`)。SubOrche 已确认并回信 `toZhihuWatcherV11Impler/from-zhihucrawlorche-v11-decisions.md`:接受 v1.1 + 批 §知乎链路 全局提升 A/B/C(impler 落地给 diff→我验→收敛)+ greenlight「默认收藏夹自动分类」=新 SP（SubOrche 待 brainstorm 成 SP）| 2026-06-10 | user + zhihuwatcherv11impler + zhihucrawl suborche |
| UN-030 | 起 HarnessStackImpler session（CodeTeam #3/#4 实例化）— **done**：brainstorm 三决策（U1 全量装 codegraph + `.mcp.json`；U2 Lane Tiering 按译稿采纳；U3 Lane 声明=plan frontmatter）+ user 中途指令 U4（grill-with-docs 缺口必须补→已落首个项目级 skill `.claude/skills/grill-with-docs/`）。落地：CLAUDE.md + longterm.md 新 §Harness Enhancement Layer (v2) + §Lane Tiering (v2)（merge `ab6a71e`）；codegraph v0.9.9 装好并实证（128 文件索引）；判定=additive amendment 非 Full Rewrite；Step 8 零晋升+prune 已闭环（`e39a464`）。done 信 `from-harnessstackimpler-done.md`；用户余项转 UN-031 | 2026-06-10 | user + harnessstackimpler |
| UN-028 | 决定 SP-2 v1.2 方向（blocker）— **done**：user 选 **A**（引擎加固，守 D1 无签名、拒 B 签名器）并加码要求"先做主动限流避免被抓 + 被拒重试"。impler 安全测得限流为突发敏感（60 连续 nav-GET @~2req/s 零限流）→ 重定范围为「统一 `_request` 加主动限流 + 403/429 退避重试（遵从 Retry-After）」，plan 已落 `Engine/zhihu/docs/superpowers/plans/2026-06-09-zhihu-ratelimit-hardening-plan.md`；进度走 §SP Status Board SP-2 行（v1.2 🟡 wip） | 2026-06-09 | user + zhiharticleimpler |
| UN-027 | 起 ZhihuArticleImpler session（SP-2 v1.2）— **done**：会话已起并跑完 RepoMem.read 两层 + 读 §2/§3 代码缝 + green baseline（58 tests）+ §4 gating live-probe。probe 推翻前提（articles `/api/v4` 需签名、专栏 nav-GET 实测 200），未写代码即命中 §4 blocker → escalate `from-zhihuarticleimpler-blocker-zse96.md`，user 新动作转 **UN-028** | 2026-06-07 | user + zhiharticleimpler |
| UN-022 | 起 BilibiliCrawl SubOrche session（继承 Bilibili 垂直）— **done**：SubOrche 已 boot，compressed brainstorm 锁定 SP-4b/SP-5b 两个 B站特有跨 SP 决策——(1) SP-4b LLMClient 跨垂直依赖已解除(SP-3 done、真实 LLMClient+mimo 已配 → 直接复用)、(2) SP-5b 水位按用户指令改"Stage 0 真站爬 API → 字段文档待 user review → 才实现"(纠正 SP-5a armchair 误读 `content.created`)；产出 `toSP4bImpler/`+`toSP5bImpler/` 两封 handoff；用户新动作转 UN-025/UN-026。SP-5a 顶层 `created`=收藏时间的纠正(跨垂直、出 SubOrche scope)**已按 user 决定升级 root g4** → `toOrchestrator/from-bilibilicrawlorche-sp5a-watermark-correction.md`,建议折进 SP-5a v1.1(UN-024) | 2026-06-07 | user + bilibilicrawl suborche |
| UN-023 | 起 g4 root orche session 继承编排 — **done**：g4（Claude Opus 4.8 1M）已读 `g4-handoff.md`、跑完引导检查（git log + 三个 SubOrche 收件箱 + 五大 must-read）、确认 SP-3/SP-5a 已 done+merged 且 SP-3 LLM 凭据 gate 已解（live `mimo-v2.5-pro`）；g3 退场，g4 接管治理 + 两条 SubOrche 线 + SP-6/7 排程；`g4-handoff.md` 已 burn，version-plan generations g4 行已标识 | 2026-06-07 | user + orche g4 |
| UN-021 | 起 SP5aImpler session — **done**：会话已起并在跑;Stage 1–4(design+plan)已落、user 批准;进度跟踪走 §SP Status Board SP-5a 行 | 2026-06-02 | user + sp5aimpler |
| UN-020 | 起 SP3Impler session — **done**：SP-3 已在跑（LLMClient 真实现已落、interface 冻结、code-review 完；近完成，卡用户 LLM 凭据做 live smoke）；进度走 §SP Status Board SP-3 行 | 2026-06-07 | user + sp3impler |
| UN-018 | 起 BiliNote docker + 确认 endpoint — **done**：用户起容器（compose 映射后端 `:8483`，因 `latest` 镜像 nginx 坏）；SP4aImpler 经 API 配 `TRANSCRIBER_TYPE=bcut` + 注册 mimo-v2.5-pro 供应商，跑通 ASR + 字幕两条路径 smoke。SP-4a 已 done | 2026-06-02 | user + sp4aimpler |
| UN-019 | 起 ZhihuCrawl SubOrche session（继承 Zhihu 垂直）— **done**：SubOrche 已 boot，compressed brainstorm 锁定 SP-3/SP-5a 跨 SP 边界（cookie=主动 pull、输出=可配置根 vault 无关、LLMClient 真实现归 SP-3）；产出 `toSP3Impler/`+`toSP5aImpler/` 两封 handoff + cookie-pull 决策升级信给 root（`from-zhihucrawlorche-cookie-pull-decisions.md`）；用户新动作转 UN-020/UN-021 | 2026-06-02 | user + zhihucrawl suborche |
| UN-001 | 授权全局安装 OpenSpec CLI（`npm i -g @fission-ai/openspec` → v1.3.1） | 2026-05-27 | bootstrap session |
| UN-002 | 把 handoff 带到 `~/Codes/AgentCrawlers/` 起 ruflo 会话 — **obsoleted**：2026-05-30 决定继续在当前 repo 开发，迁移取消 | 2026-05-30 | bootstrap session |
| UN-003 | 起新 orchestrator 会话，按 handoff §7 执行 — **completed**：scope 扩展到 10 子项目，SP-0 design + plan 已落盘 | 2026-05-31 | orche session 2 |
| UN-004 | 确认已开通阿里云 AK/SK + Tingwu + OSS bucket — **obsoleted**：R5 (2026-05-31) 决定切换 BN+bcut，v1 不再依赖 Aliyun 凭据 | 2026-05-31 | orche session 2 |
| UN-007 | 起 SP-0 impler — **done**：SP-0 完整落地（tag `sp0-complete` at `5c28447`）；handoff chain 已 burn | 2026-05-31 | sp0impler |
| UN-009 | 起 SP-1 impler — **done**：SP1Impler 已在跑（Stage 1 wip）；进度跟踪走 `§SP Status Board` SP-1 行，无 user 动作待办 | 2026-05-31 | user + sp1impler |
| UN-010 | 决定 SP-0 final-sweep residue 处理范围 — **resolved**：orche decisions letter ack D1=A（4 文件改 v2）+ D2=A（AgentCrawl 串延到 UN-005）；sp0impler 已执行（commit `de4af04`） | 2026-05-31 | sp0impler + orche |
| UN-005 | 物理改名 `Tools/AgentCrawl/` → `Tools/JarvanKB/` — **done**（user 独立 session 执行；D2 deferred AgentCrawl 串改名 orche 跟进于本 commit，仅历史 narrative 文件保留旧名） | 2026-05-31 | user + orche |
| UN-011 | 起 g3 orche session 继承编排 — **done**：g3（Claude Opus 4.8 1M）已读 g3-handoff、跑完引导检查、接管 SP1Impler 反馈链路；g2 退场；inheritance handoff 已 burn | 2026-05-31 | user + orche g3 |
| UN-012 | 给 SP-1 Stage 3 greenlight + 选执行模式 — **resolved**：user 在 SP1Impler chat 直接给绿灯 + 选 subagent-driven；orche g3 plan 审阅通过（fork→自写偏离由 CookieCloud GPLv3 发现正当化，保 MIT 干净）。SP1Impler 已进入 Stage 3 | 2026-05-31 | user + orche g3 |
| UN-013 | cookie-manager 公网暴露 — **done**：公网 HTTPS 全链路验证通（`https://www.zhaoricheng.fun:48098` / 直连 `:48088`）；frps AI 配了 Nginx SSL 代理。剩余"起正式服务 + 扩展填 HTTPS 地址"是用户例行操作，非 orche 待办 | 2026-05-31 | sp1impler + frps AI + user |
| UN-014 | SP-1 sendbox 后处理 — **done**：archive sp0-done、burn sp1-done + toSP1Impler/handoff + toFRPS/handoff；Step 8 merge 由 impler 完成（未重做）；`temp/sp1-cookie-manager/research.md` 保留（impler 刻意保留 + 下游 SP-2/3 参考） | 2026-06-01 | orche g3 |
| UN-015 | 修订 merge 归属规范 — **done**：CLAUDE.md §3 step8 + §4、longterm §Pipeline v2 + §Hard Invariants 均加"impler owns merge closure"，并固化 handoff §3.F 措辞（commit `16da3b6`） | 2026-06-01 | orche g3 |
| UN-016 | 起 SP2Impler session — **done**：SP-2 知乎引擎完成并 merge（`f8c14cb`，51 tests + 真站 smoke）；impler 闭环 Step 8（提升知乎链路根因到 crawl-pipeline.md）；orche 已 burn SP-2 sendbox 链 + codify promotion standard（`96c9548`） | 2026-06-02 | user + sp2impler + orche g3 |
| UN-017 | 起 SP4aImpler session — **done**：SP4aImpler 已在跑（Stage 2 done，plan-ready 已发）；orche g3 审阅通过 → `toSP4aImpler/from-orche-sp4a-greenlight.md`（subagent-driven）；离线 Tasks 1–14 可跑，live smoke 仍 gate 在 UN-018 | 2026-06-02 | user + sp4aimpler + orche g3 |
